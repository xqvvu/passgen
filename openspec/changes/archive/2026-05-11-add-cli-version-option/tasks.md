## 1. CLI Version Behavior

- [x] 1.1 Add CLI parsing support for `-V` and `--version`.
- [x] 1.2 Implement version output by reading the current package version without hard-coding it in CLI source.
- [x] 1.3 Ensure version output takes precedence and does not generate a password.

## 2. Documentation And Tests

- [x] 2.1 Document `-V, --version` in README usage/options.
- [x] 2.2 Add colocated `node:test` coverage for `-V` and `--version`.
- [x] 2.3 Run lint, build, tests, OpenSpec validation, and npm pack dry run.
